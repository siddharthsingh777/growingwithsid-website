# Port the API server runs on
PORT=4000

# Simple key required to view subscribers/messages via the admin endpoints
# Change this to something private before deploying
ADMIN_KEY=change-me-super-secret

# Comma-separated list of allowed frontend origins (CORS).
# Use * during local development, then lock this down after deploying.
ALLOWED_ORIGINS=*
